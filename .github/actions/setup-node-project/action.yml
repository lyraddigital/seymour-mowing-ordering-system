name: Setup Node Project

description: Configure Node and install project dependencies.

runs:
  using: composite

  steps:
    - name: Set up Node
      uses: actions/setup-node@v7
      with:
        node-version-file: .nvmrc
        cache: npm

    - name: Install dependencies
      shell: bash
      run: npm ci